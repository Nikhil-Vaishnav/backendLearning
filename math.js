const sum = (a,b) => a + b;   
const mul = (a , b) => a * b;
//  require() a built-in function to include external modules that exits in separete fies.
// module.exports - a special object id used to export.

module.exports = 123;/*this will export to any file which neede*/

